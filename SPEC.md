# 人力排期管理工具 - 产品规格说明 (SPEC)

## 1. 产品概述

### 1.1 产品名称
人力排期管理工具 (Team Capacity Planner)

### 1.2 产品定位
本地 Web 应用，用于团队管理者可视化管理组内人员的项目分配和工作负载。

### 1.3 目标用户
- 团队负责人 / 项目经理
- 需要同时管理 6-20+ 人的项目分配

### 1.4 核心价值
- **谁有空？** — 快速识别可用人力
- **项目吃了多少人力？** — 量化项目资源消耗
- **每个人在干什么？** — 全景视图掌握团队分配

---

## 2. 功能规格

### 2.1 数据模型

| 实体 | 字段 | 说明 |
|------|------|------|
| Person | id, name, color | 团队成员 |
| Project | id, name, color | 项目 |
| Allocation | id, personId, projectId, requirementName, startDate, endDate, effortPercent | 排期分配 |

**关系**：
- 一个 Person 可以有多个 Allocation（跨项目）
- 一个 Project 可以有多个 Allocation（多人参与）
- Allocation 通过 requirementName 关联到具体需求

### 2.2 核心功能

#### F1: 人员管理
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F1.1 | 添加人员 | 输入姓名，自动分配颜色 | 人员出现在侧边栏和时间轴 |
| F1.2 | 删除人员 | 删除人员及其所有分配 | 人员和关联分配全部移除 |

#### F2: 项目管理
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F2.1 | 添加项目 | 输入项目名称，自动分配颜色 | 项目出现在侧边栏 |
| F2.2 | 删除项目 | 删除项目及其所有分配 | 项目和关联分配全部移除 |

#### F3: 排期分配
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F3.1 | 创建排期 | 点击时间轴单元格，填写弹窗 | 分配色块出现在时间轴 |
| F3.2 | 查看排期 | 点击分配色块查看详情 | 弹窗显示项目、需求、时间、比例 |
| F3.3 | 编辑排期 | 在详情弹窗中点击编辑 | 可修改需求、时间、比例 |
| F3.4 | 删除排期 | 在详情弹窗中删除 | 色块从时间轴移除 |
| F3.5 | 投入比例 | 支持 10%-200%，允许超载 | 滑块可调整，显示当前值 |
| F3.6 | 按周/月自动计算结束时间 | 选择"2周"或"1个月"后自动计算结束日期 | 结束日期 = 开始日期 + 选择的周/月数 |
| F3.7 | 拖拽调整排期 | 拖拽色块移动到其他周 | 排期时间自动更新 |

#### F4: 视图切换
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F4.1 | 按人排期 | 每人一行，时间轴横向展开 | 泳道布局，多任务占多行 |
| F4.2 | 按项目排期 | 按项目→需求分组显示 | 每个需求一行，显示分配的人员 |
| F4.3 | 空闲人力 | 热力图显示各人各周负载 | 颜色编码：绿=空闲，红=超载 |

#### F5: 负载计算
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F5.1 | 周负载 | 累加某人某周的所有分配比例 | 百分比显示在单元格 |
| F5.2 | 颜色编码 | 根据负载显示背景色 | 0=绿，≤50=蓝，≤80=黄，≤100=橙，>100=红 |

#### F6: 数据管理
| 编号 | 功能 | 描述 | 验收标准 |
|------|------|------|---------|
| F6.1 | 持久化存储 | localStorage 自动保存 | 刷新页面数据不丢失 |
| F6.2 | 导出 JSON | 下载 JSON 备份文件 | 文件包含完整数据 |
| F6.3 | 导入 JSON | 从 JSON 文件恢复数据 | 数据正确加载到系统 |
| F6.4 | 清空数据 | 一键清空所有数据 | 有确认弹窗，数据清空 |

---

## 3. 交互规格

### 3.1 时间轴
- 横轴：周（周一为起始）
- 纵轴：人员/需求
- 单元格宽度：80px
- 行高：36px/泳道

### 3.2 分配色块
- 位置：绝对定位，覆盖对应的周
- 宽度：跨越的周数 × 80px
- 内容：项目名 + 投入比例
- 交互：悬停显示详情，点击打开编辑弹窗

### 3.3 泳道布局
- 同一人的多个分配如果不重叠，放在同一泳道
- 重叠的分配放在不同泳道
- 行高随泳道数自动扩展

---

## 4. 技术规格

### 4.1 技术栈
- React 18 + TypeScript
- Vite 构建工具
- Zustand 状态管理
- Tailwind CSS 样式
- dayjs 日期处理

### 4.2 数据存储
- ✅ 已实现：localStorage 持久化（刷新不丢失）
- 数据大小限制：~5MB（足够 20人 × 6个月）
- 导出/导入：JSON 格式备份恢复

### 4.3 浏览器兼容
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 5. 非功能需求

### 5.1 性能
- 20人 × 26周 的时间轴渲染 < 100ms
- 分配操作响应 < 50ms

### 5.2 可用性
- 零培训成本，直觉操作
- 中文界面
- 响应式布局（最小宽度 1024px）

---

## 6. 未来规划

### 6.1 P1（近期）
- [x] localStorage 持久化
- [x] 编辑已有排期
- [x] 拖拽调整排期
- [x] 按周/月自动计算结束时间

### 6.2 P2（中期）
- [x] 导出 JSON
- [x] 导入 JSON
- [ ] 需求详情管理

### 6.3 P3（远期）
- [ ] 多人协作（WebSocket）
- [ ] 移动端适配
- [ ] 与项目管理工具集成
