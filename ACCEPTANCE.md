# ACCEPTANCE: 阶段分类功能

> team-capacity-planner | 2026-06-08
> 验收人：PM（产品经理视角）

---

## 验收流程

每个验收项逐条打勾，全部通过才算完成。

---

## A. 数据模型

- [ ] `types/index.ts` 新增 `Requirement` 和 `Phase` 接口
- [ ] `Allocation` 新增 `phaseId` 字段
- [ ] Zustand store 新增 `requirements`、`phases` 状态
- [ ] localStorage 保存/加载包含新字段
- [ ] `schema.ts` Dexie schema 升级（version 2）含 requirements、phases 表

## B. 侧边栏：需求/阶段管理

- [ ] 侧边栏新增「需求」管理区域
- [ ] 可选择项目 → 输入需求名称 → 添加需求
- [ ] 需求列表按项目分组显示（`项目名 / 需求名`）
- [ ] 点击需求可展开，显示阶段列表
- [ ] 可输入阶段名称 → 添加阶段
- [ ] 需求支持删除（级联删除阶段 + allocations）
- [ ] 阶段支持删除（级联删除 allocations）

## C. 排期弹窗

- [ ] 项目下拉 → 选择项目后，需求下拉过滤为该项目的需求
- [ ] 需求下拉 → 选择需求后，阶段下拉过滤为该需求的阶段
- [ ] 项目无需求时，需求下拉显示提示文字
- [ ] 需求无阶段时，阶段下拉显示提示文字
- [ ] 开始日期默认值 = 当天所在周的周一
- [ ] 时长选择器正常工作（快捷按钮 + 自定义周/月数）
- [ ] 保存后 allocation 正确关联 phaseId

## D. 甘特图色块

- [ ] 按人排期视图：色块文字 = `需求名/阶段名 投入%`
- [ ] 按项目排期视图：需求作为分组标题行（灰色背景）
- [ ] 按项目排期视图：阶段作为子数据行，每行独立显示色块
- [ ] 色块颜色仍由项目决定（不变）

## E. 统计逻辑

- [ ] 一个需求有多个阶段，需求计数 = 1
- [ ] 空闲人力视图：负荷 = 所有 allocation effortPercent 之和（阶段独立累加）
- [ ] 负荷颜色编码不变（空闲绿→超载红）

## F. 数据迁移

- [ ] 旧格式 localStorage（无 requirements/phases）自动迁移
- [ ] 迁移后 UI 显示无变化，用户无感
- [ ] 旧格式 JSON 导入自动迁移

## G. 导出/导入

- [ ] 导出 JSON 包含 requirements、phases 字段
- [ ] 导入含新字段的 JSON 正确恢复
- [ ] 导入旧格式 JSON 触发迁移

## H. 边界条件

- [ ] 删除项目 → 级联删除需求 → 阶段 → allocations
- [ ] 刷新页面后所有数据持久化
- [ ] build 通过，无 TypeScript 错误

---

## 验收标准

全部打勾 ✅ = 通过
任何一项未勾 = 打回，附具体问题描述
